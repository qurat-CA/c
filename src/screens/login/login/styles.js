import { StyleSheet } from "react-native";

import { COLORS } from "../../../globals";

export const styles = StyleSheet.create({
  divider: {
    text: "Or",
    size: 14,
    medium: true,
    color: COLORS.text,
    textAlign: "center",
  },
});
